[build-system]
requires = ["setuptools>=67.0"]
build-backend = "setuptools.build_meta"

[project]
name = "motion_planning_tethered_robots"
dynamic = ["version"]
description = "Motion planning algorithms for tethered mobile robots."
authors = [
    {name="Gianpietro Battocletti", email="g.battocletti@tudelft.nl"},
]
readme = "README.md"
keywords = [
    "tethered robot",
    "mobile robot",
    "path planning",
]
dependencies = [
    "numpy",
    # TODO
]

[project.optional-dependencies]
dev = [
    "black",
    "isort",
]

[tool.setuptools]
license-files = ["LICENSE"]

[tool.setuptools.dynamic]
version = {attr = "motion_planning_tethered_robots.__version__"}

[tool.black]
line-length = 88
exclude = 'dev/.*'

[tool.isort]
profile = "black"
py_version = 311
line_length = 88
skip = ["dev/.*"]

[tool.license]
name = "GNU General Public License v3.0"
url = "https://www.gnu.org/licenses/gpl-3.0.en.html"
